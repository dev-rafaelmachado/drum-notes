/**
 * @drum-notes/core
 *
 * Shared types, utilities and constants used across the monorepo.
 * Must not contain React or Browser-framework code (see .claude/architecture.md).
 */
export { createId } from "./id";
