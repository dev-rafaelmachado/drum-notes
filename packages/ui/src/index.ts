/**
 * @drum-notes/ui
 *
 * Shared components and design system. Must not contain business logic or
 * domain rules (see .claude/architecture.md). Components are added here via
 * shadcn/ui as the design system grows.
 *
 * Intentionally empty until the first shared component is specified.
 */
export {};
